import { UserDeletePipe } from './user-delete.pipe';

describe('UserDeletePipe', () => {
  it('create an instance', () => {
    const pipe = new UserDeletePipe();
    expect(pipe).toBeTruthy();
  });
});
