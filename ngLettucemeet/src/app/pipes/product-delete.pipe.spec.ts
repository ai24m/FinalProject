import { ProductDeletePipe } from './product-delete.pipe';

describe('ProductDeletePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductDeletePipe();
    expect(pipe).toBeTruthy();
  });
});
