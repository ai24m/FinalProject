import { MarketDeletePipe } from './market-delete.pipe';

describe('MarketDeletePipe', () => {
  it('create an instance', () => {
    const pipe = new MarketDeletePipe();
    expect(pipe).toBeTruthy();
  });
});
